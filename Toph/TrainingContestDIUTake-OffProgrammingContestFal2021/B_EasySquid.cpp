#include <bits/stdc++.h>
using namespace std;
typedef long long int ll;

int main()
{
    ios::sync_with_stdio(false);
    cin.tie(0);
    cout.tie(0);

    int n;
    cin >> n;
    if (n >= 567)
    {
        cout << "Demetris is eligible for this game!\n";
    }
    else
    {
        cout << "Better luck next time!\n";
    }

    return 0;
}
