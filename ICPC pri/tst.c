#include<stdio.h>
int main()
{
    int r = 1, nm = 5;
    int i;
    if(r==1)
        printf("   %d |", );
    if(r==2)
        printf("  %d |", );
    printf(" %s", );
    for(i = nm; i<= 11; i++)
    {
        printf(" ");
    }
    printf("| %d\n", );

}
